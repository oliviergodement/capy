<h2>Se connecter</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-group form-firm-name">
    <div><%= f.label :email, "Adresse e-mail", class: 'control-label' %><br />
    <%= f.email_field :email, autofocus: true, class: 'form-control' %></div>
  </div>

  <div class="form-group form-firm-name">
    <div><%= f.label :password, "Mot de passe", class: 'control-label' %><br />
      <%= f.password_field :password, autocomplete: "off", class: 'form-control' %></div>
  </div>

  <div class="form-group form-firm-name">
    <% if devise_mapping.rememberable? -%>
      <div><%= f.check_box :remember_me %> <%= f.label :remember_me, "Rester connecté" %></div>
    <% end -%>
  </div>

  <div><%= f.submit "Se connecter" %></div>
<% end %>

<%= render "devise/shared/links" %>
